import { SiteConfig } from 'azure-arm-website/lib/models';
import { AzureParentTreeItem, GenericTreeItem, IActionContext } from 'vscode-azureextensionui';
import { DeploymentTreeItem } from './DeploymentTreeItem';
import { ISiteTreeRoot } from './ISiteTreeRoot';
export declare class DeploymentsTreeItem extends AzureParentTreeItem<ISiteTreeRoot> {
    static contextValueConnected: string;
    static contextValueUnconnected: string;
    contextValue: string;
    parent: AzureParentTreeItem<ISiteTreeRoot>;
    readonly label: string;
    readonly childTypeLabel: string;
    private readonly _connectToGitHubCommandId;
    constructor(parent: AzureParentTreeItem<ISiteTreeRoot>, siteConfig: SiteConfig, connectToGitHubCommandId: string);
    readonly iconPath: {
        light: string;
        dark: string;
    };
    hasMoreChildrenImpl(): boolean;
    loadMoreChildrenImpl(_clearCache: boolean): Promise<DeploymentTreeItem[] | GenericTreeItem<ISiteTreeRoot>[]>;
    compareChildrenImpl(ti1: DeploymentTreeItem, ti2: DeploymentTreeItem): number;
    disconnectRepo(context: IActionContext): Promise<void>;
    refreshLabelImpl(): Promise<void>;
}
