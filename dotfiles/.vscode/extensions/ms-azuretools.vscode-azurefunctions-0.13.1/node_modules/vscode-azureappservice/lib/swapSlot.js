"use strict";
/*---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------*/
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const azure_arm_website_1 = require("azure-arm-website");
const vscode_1 = require("vscode");
const vscode_azureextensionui_1 = require("vscode-azureextensionui");
const extensionVariables_1 = require("./extensionVariables");
const localize_1 = require("./localize");
function swapSlot(sourceSlotNode, existingSlots) {
    return __awaiter(this, void 0, void 0, function* () {
        const sourceSlotClient = sourceSlotNode.root.client;
        const productionSlotLabel = 'production';
        const otherSlots = [{
                label: productionSlotLabel,
                description: localize_1.localize('swapWithProduction', 'Swap slot with production'),
                data: undefined
            }];
        for (const slot of existingSlots) {
            if (sourceSlotClient.slotName !== slot.root.client.slotName) {
                // Deployment slots must have an unique name
                const otherSlot = {
                    // tslint:disable-next-line:no-non-null-assertion
                    label: slot.root.client.slotName,
                    data: slot
                };
                otherSlots.push(otherSlot);
            }
        }
        const placeHolder = localize_1.localize('selectSlotToSwap', 'Select which slot to swap with "{0}".', sourceSlotClient.slotName);
        const targetSlot = (yield extensionVariables_1.ext.ui.showQuickPick(otherSlots, { placeHolder })).data;
        const targetSlotLabel = targetSlot ? targetSlot.root.client.fullName : `${sourceSlotClient.siteName}-${productionSlotLabel}`;
        const swappingSlots = localize_1.localize('swapping', 'Swapping "{0}" with "{1}"...', targetSlotLabel, sourceSlotClient.fullName);
        const successfullySwapped = localize_1.localize('swapped', 'Successfully swapped "{0}" with "{1}".', targetSlotLabel, sourceSlotClient.fullName);
        extensionVariables_1.ext.outputChannel.appendLine(swappingSlots);
        const client = vscode_azureextensionui_1.createAzureClient(sourceSlotNode.root, azure_arm_website_1.WebSiteManagementClient);
        yield vscode_1.window.withProgress({ location: vscode_1.ProgressLocation.Notification, title: swappingSlots }, () => __awaiter(this, void 0, void 0, function* () {
            // if targetSlot was assigned undefined, the user selected 'production'
            if (!targetSlot) {
                // tslint:disable-next-line:no-non-null-assertion
                yield client.webApps.swapSlotWithProduction(sourceSlotClient.resourceGroup, sourceSlotClient.siteName, { targetSlot: sourceSlotClient.slotName, preserveVnet: true });
            }
            else {
                // tslint:disable-next-line:no-non-null-assertion
                yield client.webApps.swapSlotSlot(sourceSlotClient.resourceGroup, sourceSlotClient.siteName, { targetSlot: targetSlot.root.client.slotName, preserveVnet: true }, sourceSlotClient.slotName);
            }
            vscode_1.window.showInformationMessage(successfullySwapped);
            extensionVariables_1.ext.outputChannel.appendLine(successfullySwapped);
        }));
    });
}
exports.swapSlot = swapSlot;
//# sourceMappingURL=swapSlot.js.map